services:
  frontend:
    build: .
    ports:
      - '8080:8080'
    # Uncomment when FastAPI backend is ready:
    # depends_on:
    #   - backend

  # backend:
  #   build: ./backend
  #   ports:
  #     - "8000:8000"
